#include <stdio.h>
#include <conio.h>

void main()
{
    char ch;
    int n, i;
    
    printf("Enter a character: ");
    scanf("%c", &ch);
    printf("Enter n: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
    {
        printf("%c ", ch + i);
    }
}
