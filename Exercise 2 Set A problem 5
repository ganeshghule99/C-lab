#include <stdio.h>

int main() {
    float a, b, AM, HM;

    printf("Enter two numbers: ");
    scanf("%f %f", &a, &b);

    AM = (a + b) / 2.0;
    HM = (a * b) / (a + b);

    printf("Arithmetic Mean: %.2f\n", AM);
    printf("Harmonic Mean: %.2f\n", HM);

    return 0;
}
