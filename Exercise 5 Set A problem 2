#include <stdio.h>
#include <conio.h>

void main()
{
    int x, y, i, sum = 0;
    printf("Enter x and y: ");
    scanf("%d%d", &x, &y);

    for(i = x; i <= y; i++)
    {
        sum = sum + i;
    }
    
    printf("Sum is %d", sum);
}

