#include <stdio.h>
#include <conio.h>

void main()
{
    int sum = 0, n;
    
    printf("Enter the value of n: ");
    scanf("%d", &n);
    
    while(n > 0)
    {
        sum = sum + n;
        n--;
    }
    
    printf("\nThe sum of numbers is %d", sum);
}

